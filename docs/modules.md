# Modules

The package is organized under `src/airouter`.

## Core modules

- `cli.py`: CLI entrypoints and command wiring
- `router.py`: model selection logic
- `scorer.py`: request scoring rules
- `registry.py`: model discovery and install helpers
- `context.py`: prompt and repository context feature extraction
- `ollama.py`: Ollama installation and inference streaming helpers
